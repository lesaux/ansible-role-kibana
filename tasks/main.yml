---
- include: setup-RedHat.yml
  when: ansible_os_family == 'RedHat'

- include: setup-Debian.yml
  when: ansible_os_family == 'Debian'

- name: Install Kibana.
  package:
    name: "{{ kibana_package }}"
    state: "{{ kibana_package_state }}"

- name: add kibana user to elasticsearch group
  user:
    name: kibana
    groups: elasticsearch
    append: yes

- name: configure log folder
  file:
    path: {{ kibana_loggin_dest | basename }}
    state: directory
    owner: kibana
  when: {{ kibana_loggin_dest }} =! "stdout"

- name: configure log rotate
  file:
    path: /etc/logrotate.conf.d/kibana
    state: file
    owner: root
    content: |
      {{ kibana_loggin_dest | basename }} {
        missingok
        notifempty
        sharedscripts
        postrotate
        /bin/kill -HUP $(cat /var/run/kibana.pid 2>/dev/null) 2>/dev/null
        endscript
      }
  when: {{ kibana_loggin_dest }} =! "stdout"

- name: Copy Kibana configuration.
  template:
    src: "{{ kibana_config_template }}"
    dest: "{{ kibana_config_file_path }}"
    owner: root
    group: root
    mode: 0644
  notify: restart kibana

- name: Ensure Kibana is started and enabled at boot.
  service:
    name: kibana
    state: "{{ kibana_service_state }}"
    enabled: "{{ kibana_service_enabled }}"

